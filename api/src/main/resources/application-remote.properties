# 원격 DB(Tailscale, 100.125.247.64) 접속용 프로필.
# 실제 계정/비밀번호는 여기 적지 않고 환경변수로 주입한다(08_개발표준.txt 참고).
# 실행 전 DB_URL / DB_USER / DB_PASSWORD 환경변수를 먼저 설정할 것.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

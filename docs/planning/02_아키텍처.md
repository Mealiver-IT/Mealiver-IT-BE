# Architecture — 도메인 모델 및 동시성 제어 전략

관련: [01_기획서.md](./01_기획서.md) | [03_시스템설계.md](./03_시스템설계.md) | [04_기술문서.md](./04_기술문서.md) | [06_일정과역할.md](./06_일정과역할.md) | [05_개발표준.md](./05_개발표준.md) | [07_ERD.dbml](./07_ERD.dbml)

## 1. 도메인 모델

```
User (mock, 회원가입/로그인 없음)
 ├─ id (PK, BIGINT)
 ├─ login_id (VARCHAR, UNIQUE) -- 가상 식별자
 ├─ name (VARCHAR)            -- PII, 마스킹 대상
 ├─ phone (VARCHAR)           -- PII, 마스킹 대상
 ├─ email (VARCHAR)           -- PII, 마스킹 대상
 ├─ membership_tier (ENUM: PRIVATE, PFC, CORPORAL, SERGEANT, DEFAULT PRIVATE)  -- 이등병/일병/상병/병장, orders 집계 기반 매월 1일 배치 자동 산정 (01_기획서.md 6.2절, 6절 하단 참고)
 ├─ tier_calculated_at (DATETIME, NULLABLE)  -- 마지막 계급 재산정 배치 실행 시각
 └─ created_at

Orders (계급 산정용 결제완료 이력. 실제 음식주문 UI는 정적 mockup, orders는 그 뒤에서 계급 계산만을 위해 존재하는 최소 스키마)
 ├─ id (PK, BIGINT)
 ├─ user_id (FK)
 ├─ order_amount (DECIMAL)     -- 상품 금액(할인 전)
 ├─ paid_amount (DECIMAL)      -- 결제 금액(할인 후) — 계급 집계 10,000원 기준 컬럼
 ├─ status (ENUM: COMPLETED, CANCELED, REFUNDED)
 ├─ ordered_at (DATETIME)
 ├─ completed_at (DATETIME, NULLABLE)  -- 배달완료 시각, 계급 집계 기준
 └─ created_at

MembershipTierLog (계급 재산정 배치 감사로그 — CouponStateLog와 동일 패턴)
 ├─ id (PK)
 ├─ user_id (FK)
 ├─ from_tier / to_tier (ENUM)
 ├─ order_count (INT)   -- 해당 배치가 집계한 완료 주문 건수
 └─ calculated_at (DATETIME)   -- 배치 실행 시각(매월 1일)

Campaign
 ├─ id (PK, BIGINT)
 ├─ name (VARCHAR)
 ├─ total_stock (INT)          -- 최초 재고, 불변
 ├─ remaining_stock (INT)      -- 동시성 제어 대상 카운터 (전략에 따라 사용 여부 다름)
 ├─ open_at (DATETIME)         -- 예약 오픈 (선택 확장)
 ├─ close_at (DATETIME)
 ├─ status (ENUM: READY, OPEN, CLOSED)
 ├─ min_membership_tier (ENUM, NULLABLE)  -- 회원 전용 쿠폰 eligibility, NULL이면 전 회원 대상
 └─ version (BIGINT)           -- @Version, 낙관적 락 옵션용

Coupon (캠페인이 발급하는 쿠폰 정책 마스터. 캠페인:쿠폰 1:1로 단순화 가능)
 ├─ id (PK)
 ├─ campaign_id (FK)
 ├─ discount_type (ENUM: FIXED, RATE)
 ├─ discount_value (DECIMAL)   -- RATE 타입 + 계급별 차등 캠페인의 경우 기본값(이등병/일병 기준). 실제 적용률은 TierDiscountPolicy(6절) 참고
 ├─ min_order_amount (DECIMAL, NULLABLE)  -- 최소 주문금액 조건부 할인 (NULL이면 조건 없음)
 └─ valid_days (INT)           -- 발급일로부터 유효기간

CouponIssue (발급 이력 = 상태 관리 핵심 테이블, 300만 건 대상)
 ├─ id (PK, BIGINT, AUTO_INCREMENT)
 ├─ campaign_id (FK)
 ├─ user_id (FK)
 ├─ coupon_code (VARCHAR, UNIQUE)      -- 발급된 실물 쿠폰 식별자
 ├─ discount_type (ENUM: FIXED, RATE)  -- 발급 시점 스냅샷
 ├─ discount_value (DECIMAL)           -- 발급 시점 계급으로 확정된 실제 할인율/금액 스냅샷. 이후 유저 계급이 바뀌어도 이 값은 불변(FR-FCFS-002)
 ├─ status (ENUM: ISSUED, USED, CANCELED, EXPIRED)
 ├─ idempotency_key (VARCHAR, UNIQUE)  -- 발급 요청 중복 방지 키
 ├─ issued_at (DATETIME NOT NULL)
 ├─ used_at (DATETIME NULL)
 ├─ canceled_at (DATETIME NULL)
 ├─ expired_at (DATETIME NULL)
 ├─ version (BIGINT NOT NULL DEFAULT 0)  -- @Version, 상태전이 낙관적 락
 └─ created_at / updated_at

CouponStateLog (상태전이 감사로그 — 검증 배치가 "이력 vs 카운터" 비교할 근거)
 ├─ id (PK)
 ├─ coupon_issue_id (FK)
 ├─ from_status
 ├─ to_status
 ├─ request_id (VARCHAR)   -- 상태변경 요청의 idempotency key
 └─ created_at
```

## 2. 필수 제약조건 (Defense in Depth)

어떤 동시성 전략을 앱 레벨에서 선택하든, DB 제약이 최후 방어선 역할을 한다.

```sql
-- 1인 1매 보장의 최종 방어선: 어떤 동시성 전략을 쓰든 이 제약이 없으면 초과발급 가능
ALTER TABLE coupon_issue
  ADD CONSTRAINT uk_campaign_user UNIQUE (campaign_id, user_id);

-- 발급 요청 중복(재시도, 더블클릭, 네트워크 재전송) 방지
ALTER TABLE coupon_issue
  ADD CONSTRAINT uk_idempotency_key UNIQUE (idempotency_key);

-- 쿠폰 코드 자체의 유일성
ALTER TABLE coupon_issue
  ADD CONSTRAINT uk_coupon_code UNIQUE (coupon_code);

-- 상태전이 동시 중복요청에 대한 낙관적 락
ALTER TABLE coupon_issue ADD COLUMN version BIGINT NOT NULL DEFAULT 0;
```

`uk_campaign_user`가 핵심이다. 애플리케이션 레벨 동시성 제어(Redis, Lua, 분산락 등)가 버그로 뚫리더라도, 이 유니크 제약이 있으면 **DB가 물리적으로 2번째 INSERT를 거부**한다. 재고 초과(수량 초과) 자체는 이 제약만으로는 못 막으므로 아래 3절의 전략이 필요하지만, "1인 1매"는 이 제약 하나로 100% 보장된다.

## 3. 상태 머신

```java
public enum CouponStatus {
    ISSUED, USED, CANCELED, EXPIRED;

    private static final Map<CouponStatus, Set<CouponStatus>> TRANSITIONS = Map.of(
        ISSUED,   Set.of(USED, CANCELED, EXPIRED),
        USED,     Set.of(CANCELED),   // 사용 후 취소(환불)만 허용, 그 외 역행 불가
        CANCELED, Set.of(),
        EXPIRED,  Set.of()
    );

    public boolean canTransitionTo(CouponStatus target) {
        return TRANSITIONS.getOrDefault(this, Set.of()).contains(target);
    }
}
```

## 4. 동시성 제어 전략 비교 (팀 회의 안건)

재고 초과 방지(수량 제어)를 위한 5가지 후보. "1인 1매"는 2절의 unique constraint로 전략과 무관하게 보장되므로, 아래 비교는 **"재고 카운트 차감의 원자성을 어떻게 보장할 것인가"**에 초점을 둔다.

| 전략 | 정합성 보장 | 처리량 | 구현 복잡도 | 인프라 의존성 | 실패 모드 |
|---|---|---|---|---|---|
| **(a) DB unique constraint + 비관적 락** (`SELECT ... FOR UPDATE`) | 강함. 트랜잭션 내 row lock으로 완전 직렬화 | 낮음. 락 경합 시 대기 큐 발생, 재고 row 하나에 모든 요청이 몰림(hot row) | 낮음. `@Lock(PESSIMISTIC_WRITE)` 한 줄 | MySQL만 있으면 됨 | 락 대기 타임아웃, 데드락 가능성, DB 커넥션 풀 고갈 위험 |
| **(b) DB unique constraint + 낙관적 락/재시도** (`@Version`) | 강함이나 재시도 로직 필수 | 중간. 경합 심하면 재시도 폭증(thundering herd)으로 오히려 저하 | 중간. `spring-retry` `@Retryable` + 백오프 필요 | MySQL만 있으면 됨 | 재시도 스톰, 재시도 한도 초과 시 사용자 응답 실패 |
| **(c) Redis 원자적 감소** (`DECR` 또는 Lua script)를 DB 쓰기 앞단 게이트키퍼로 사용 | 강함(단일 스레드 원자성). Redis-DB 간 정합성은 별도 검증 필요 | 높음. 초당 수만 건 처리 가능, DB는 통과된 요청만 받음 | 중간. Lua script + 실패시 롤백(재고 복구) 로직 필요 | Redis 필수 | Redis 장애 시 전체 발급 중단, Redis-DB 불일치(보상 트랜잭션 필요) |
| **(d) Redis + 메시지 큐(Kafka)로 비동기 분리** | 강함(게이트는 c와 동일) + 처리량/내구성 향상. eventual consistency 발생 | 매우 높음. 트래픽 스파이크를 큐가 흡수 | 높음. Kafka 운영, Consumer 멱등성, 순서보장, DLQ 설계 필요 | Redis + Kafka | 컨슈머 지연/장애 시 시차 발생, 큐 적체, 메시지 유실/중복 |
| **(e) Redisson 분산락** (`RLock`) | 강함. 캠페인 단위 락으로 임계구역 직렬화 | 낮음~중간 | 중간. lease time/watchdog 튜닝, 락 해제 누락 방지 필요 | Redis 필수 | Redis 장애 시 락 획득 불가, 타임아웃 미스로 중복 통과 가능성 |
| **(f) Redis 이중 카운터** (`countReq`/`count` 분리 INCR) | 강함. `count`(2차 확정 카운터)가 독립적으로 자체 롤백하여 총 발급량이 `totalStock`을 절대 못 넘음(증명됨) | 높음. Lua 대비 오버헤드 낮음(올리브영 실측: Lua -21% vs 이중카운터 -8%) | 중간. 카운터 2개 관리 + 2차 실패 시 `count`만 롤백하는 로직 | Redis 필수(마스터 단일노드) | `countReq`만 소모되고 `count`는 실패하는 케이스가 쌓이면 재고가 남았는데도 신규 유저가 조기에 "품절" 처리됨(UX/공정성 이슈, 초과발급과는 무관) |

### 4.1 권장안 — 3단계 진행 (V1 → V2.0 → V2.1)

**V1.0 / MVP: (a) DB unique constraint + 비관적 락**
- 별도 인프라(Redis) 없이 MySQL만으로 "초과발급 0건"을 가장 확실하게, 가장 적은 코드로 증명 가능.
- 평가 핵심이 "정확성"이지 "처리량"이 아니므로, 우선 정확성을 낮은 복잡도로 확보하고 부하테스트로 입증하는 것이 리스크가 가장 낮음.
- k6로 hot row 경합(지연시간 증가, 타임아웃)을 수치로 남겨 다음 버전으로 넘어가는 근거로 사용.

**V2.0 / 하드닝 1단계: (c) Redis Lua script 게이트 — 검토 후 대체**
- (a)의 DB 커넥션/락 병목은 해소되지만, Lua script 자체의 오버헤드(올리브영 실측 -21%)와 배민 빼빼로데이 반례(복제 노드 참조 시 정합성 깨짐)가 리스크로 확인됨.
- 이 버전은 "시도했고 문제를 발견했다"는 근거로 남기고, 최종 채택은 (f)로 이동.

**V2.1 / 하드닝 최종(채택): (f) Redis 이중 카운터**
- `countReq`(1차 저비용 필터) → `count`(2차 확정, 실패 시 자체 롤백) 구조로 Lua보다 가볍게 원자성 유지.
- **countReq 조기소진 이슈**(1인1매 위반·idempotency 충돌로 죽는 요청도 `countReq`는 이미 소모)를 자체 발견·검증함. 초과발급 문제는 아니고 UX/공정성 문제로 확인, **C안(감수)** 채택 — 채점기준(초과발급 0건)이 이 케이스를 보지 않고, 메인 부하테스트 시나리오(서로 다른 유저 20,000명)에서는 애초에 발현 안 됨. 근거는 [08_리서치요약_쿠폰동시성사례.md](./08_리서치요약_쿠폰동시성사례.md) 3절 참고.
- DB unique 제약(2절)이 최종 방어선인 것은 모든 버전에서 동일.

**V3 / 검토 후 기각: 재고+중복 통합 Lua (알리바바 패턴)**
- 장애 시 영향범위가 재고+중복 둘 다 커지고 구현 복잡도만 상승 → 실제 채택은 안 하고 "검토했으나 기각" 발표 카드로만 사용.

**(d) Kafka 비동기 분리와 (e) Redisson**은 선택 확장으로 남김. (d)는 eventual consistency가 "즉시 정합성 검증" 평가 포인트와 설명 부담이 크고, (e)는 Kleppmann(2016)의 fencing token 부재 비판으로 정합성 목적에 부적합해 기각.

**확정: V1.0=(a) 비관적 락 → V2.0=(c) Redis Lua(검토 후 대체) → V2.1=(f) 이중 카운터(최종 채택).**

## 5. Idempotency 설계

### 5.1 발급(Issue) — Idempotency Key 기반

클라이언트는 요청마다 `Idempotency-Key` 헤더(예: `{userId}-{campaignId}` 또는 UUID)를 전송. `coupon_issue.idempotency_key`에 UNIQUE 제약으로 동일 키 재요청 시 DB가 두 번째 INSERT를 거부.

```java
@Transactional
public IssueResult issue(Long userId, Long campaignId, String idempotencyKey) {
    // 1) 이미 처리된 요청인지 먼저 확인 (조회로 빠른 반환)
    Optional<CouponIssue> existing = couponIssueRepository.findByIdempotencyKey(idempotencyKey);
    if (existing.isPresent()) {
        return IssueResult.alreadyProcessed(existing.get()); // 재요청도 동일 응답
    }

    // 2) 멤버십 eligibility 체크 (회원 전용 쿠폰) — 재고 확보보다 먼저, 락 경합 유발하지 않음
    Campaign campaign = campaignRepository.findById(campaignId).orElseThrow();
    if (!campaign.isEligible(userMembershipTier(userId))) {
        throw new MembershipTierNotEligibleException();
    }

    // 3) 재고 확보 (버전에 따라 DB 비관적락 / Redis Lua / Redis 이중카운터)
    boolean reserved = stockReservationService.reserve(campaignId);
    if (!reserved) throw new SoldOutException();

    try {
        // 4) INSERT — uk_campaign_user, uk_idempotency_key 가 최종 방어선
        CouponIssue issue = CouponIssue.issuedNow(userId, campaignId, idempotencyKey);
        couponIssueRepository.save(issue);
        return IssueResult.success(issue);
    } catch (DataIntegrityViolationException e) {
        // unique 제약 위반 = 동시에 같은 요청/유저가 통과됨 → 재고 원복 후 기존 레코드 반환
        stockReservationService.rollback(campaignId);
        return IssueResult.alreadyProcessed(
            couponIssueRepository.findByCampaignIdAndUserId(campaignId, userId).orElseThrow());
    }
}
```

### 5.2 상태전이(사용/취소/만료) — 상태 머신 + 낙관적 락

```java
@Transactional
public void use(Long couponIssueId, String requestId) {
    // request_id 로 CouponStateLog 조회 → 이미 처리된 요청이면 즉시 리턴 (idempotent)
    if (couponStateLogRepository.existsByRequestId(requestId)) return;

    CouponIssue issue = couponIssueRepository.findById(couponIssueId).orElseThrow();
    if (!issue.getStatus().canTransitionTo(CouponStatus.USED)) {
        throw new InvalidStateTransitionException(issue.getStatus(), CouponStatus.USED);
    }
    issue.markUsed(); // 내부에서 status=USED, used_at=now, version++ (JPA @Version 자동 관리)
    couponStateLogRepository.save(new CouponStateLog(issue.getId(), issue.getStatus(), USED, requestId));
    // save 시점에 낙관적 락 충돌(OptimisticLockingFailureException) 발생하면 spring-retry로 재시도
}
```

`@Version` 컬럼으로 동시 상태변경 요청 중 하나만 성공하고 나머지는 `OptimisticLockingFailureException` → `spring-retry`의 `@Retryable(retryFor = OptimisticLockingFailureException.class, maxAttempts = 3, backoff = @Backoff(delay=50, multiplier=2))`로 재시도 후 최신 상태 기준 재판정. `request_id`(=idempotency key) 유니크 제약을 `coupon_state_log`에도 걸어 상태변경 API도 발급과 동일한 멱등 패턴을 따르게 한다.

## 6. 멤버십 Eligibility (회원 전용 쿠폰)

`MembershipTier` enum은 `PRIVATE < PFC < CORPORAL < SERGEANT` 순서를 가지며(이등병/일병/상병/병장, [01_기획서.md](./01_기획서.md) 6절), `Campaign.isEligible(tier)`는 `min_membership_tier`가 NULL이면 항상 true, 아니면 `tier.ordinal() >= minMembershipTier.ordinal()`로 판정하는 순수 함수다.

**동시성 전략과의 관계**: eligibility 체크는 5.1절 `issue()`의 2단계로, 재고 확보(락 획득) *이전에* 수행한다. 따라서 등급 미달 요청은 락 경합에 아예 진입하지 않고 즉시 거부되어, 전략 (a)/(c)/(f) 어느 버전을 쓰든 동시성 설계에 영향을 주지 않는다. `campaign.min_membership_tier`는 캠페인 생성 시 고정되는 값이라 `campaign` row 자체에 캐시해도 되고, Redis 게이트(전략 c/f) 채택 시에는 Lua script/이중카운터 INCR 진입 전 애플리케이션 레벨에서 먼저 걸러도 된다.

### 6.1 TierDiscountPolicy — 계급별 차등 할인율 (확정)

RATE 타입 쿠폰은 발급 시점 유저 계급에 따라 할인율이 달라진다([01_기획서.md](./01_기획서.md) 2절):

| 계급 | 할인율 |
|---|---|
| 이등병 / 일병 | 10% |
| 상병 | 30% |
| 병장 | 50% |

이 매핑은 캠페인마다 달라지는 값이 아니라 프로젝트 전역의 고정 정책이므로, 별도 테이블 없이 애플리케이션 레벨 상수(`TierDiscountPolicy.rateFor(MembershipTier)`)로 구현한다.

```java
public class TierDiscountPolicy {
    public static BigDecimal rateFor(MembershipTier tier) {
        return switch (tier) {
            case PRIVATE, PFC -> new BigDecimal("0.10");
            case CORPORAL -> new BigDecimal("0.30");
            case SERGEANT -> new BigDecimal("0.50");
        };
    }
}
```

**eligibility 체크(6절 본문) 다음, 재고 확보 이전 단계**에서 `TierDiscountPolicy.rateFor(userTier)`로 실제 적용 할인율을 확정하고, `CouponIssue.discount_value`에 스냅샷으로 저장한다(1절 CouponIssue 모델 참고). 이후 유저 계급이 바뀌어도 이미 발급된 쿠폰의 `discount_value`는 갱신하지 않는다 — 발급 로직이 매번 `Campaign`/`User`를 다시 조회하는 게 아니라 발급 시점에 값을 못 박아 저장하기 때문에 별도 처리 없이 자연히 불변이 보장된다.

### 6.2 계급 산정 배치 — `MembershipTierBatchJob`

`user.membership_tier`는 더 이상 고정값이 아니라 `orders`(1절 도메인 모델) 집계로 매월 1일 재산정되는 파생값이다. 산정 규칙(완료 주문 건수 구간, 결제금액 10,000원 기준, 캘린더 월 윈도우)은 [01_기획서.md](./01_기획서.md) 6.2절 참고. 배치는 `MembershipTierLog`에 `from_tier`/`to_tier`/`order_count`를 남겨 재실행 시 동일 결과가 나오는지 감사할 수 있게 한다(재산정 SQL/배치 구조 상세는 [03_시스템설계.md](./03_시스템설계.md) 참고). 이 배치는 발급 동시성 전략(4절)과 독립적으로 동작하며, 계급 값 자체는 5.1절에서 조회 시점 스냅샷으로만 사용되므로 배치 타이밍이 발급 트랜잭션에 락 경합을 유발하지 않는다.
